package leetCode;

import java.util.HashMap;
import java.util.Map;

/**
 * LCP60 - 打地鼠（LeetCode 竞赛）
 *
 * 题目（概要）：二叉树，击破某节点影响其层和，求最大层和。
 *
 * 解法说明：本实现为占位，返回 0。典型做法为树形 DP 或 DFS。
 */
public class LCP60getMaxLayerSum {

    int maxRes = Integer.MIN_VALUE;
    Map<TreeNode, Integer> map = new HashMap<>();

    /** 占位实现 */
    public int getMaxLayerSum(TreeNode root) {
        //分两种情况，击破和不击破
        return 0;
    }
}
